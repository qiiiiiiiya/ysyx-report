
make run ARGS="--elf dummy.elf"





git branch显示都有哪些分支，要在正确的目录下，比如我的就在ysyx-workbench下面创建的分支
Ctrl+R：撤销撤销U

要将 `ps_2` 目录下的所有文件和子目录完整复制到 `pss2` 目录，需要使用 `cp` 命令的递归复制选项 `-r`（或 `-R`），具体命令如下：
### 步骤1：确保目标目录 `pss2` 存在（如果不存在则创建）
如果 `pss2` 目录还没创建，先执行以下命令创建：
```bash
mkdir -p pss2
```
`-p` 选项确保如果父目录不存在也会一起创建，这里直接创建 `pss2` 本身。


### 步骤2：递归复制 `ps_2` 下的所有内容到 `pss2`
使用 `cp -r` 递归复制目录及内容，命令：
```bash
cp -r ps_2/. pss2/
```

tmux无法滑动解决方案：Ctrl+B+冒号 输入：set -g mouse on 再回车

创建分支，合并分支，查看分支
git branch PA1
git branch 
拉取最新代码：git pull origin PA1
git merge PA2

在 Python 虚拟环境中，退出虚拟环境的方法非常简单，直接在终端中执行以下命令即可：
bash
deactivate

api:sk-ykE9gDFTfJ8izarsuJyCUhAFCFO9XbiLG9MweMnh8xFpAKd3
deepseek:sk-ed561a1063414565909289d89a6ff2d9  

python3 -m venv myenv
sudo python3 ./botsh.py
